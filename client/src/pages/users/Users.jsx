import React from 'react';
import UserDirectory from '../users/UserDirectory';

// Using the already built UserDirectory from Phase 15 as our Users module
const Users = () => {
  return <UserDirectory />;
};

export default Users;
